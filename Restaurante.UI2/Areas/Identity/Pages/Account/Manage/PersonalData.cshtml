@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Datos personales";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="container my-5">
    <h3 class="text-center mb-4">@ViewData["Title"]</h3>

    <div class="card shadow-sm p-4">
        <p class="mb-3">
            Tu cuenta contiene datos personales que nos has proporcionado. Desde esta página puedes descargar o eliminar esa información.
        </p>
        <p class="alert alert-warning mb-3">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>
            <strong>Eliminar estos datos borrará permanentemente tu cuenta y no podrá ser recuperada.</strong>
        </p>

        <div class="d-flex gap-3">
            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="flex-grow-1">
                <button class="btn btn-primary w-100">
                    <i class="bi bi-download me-1"></i> Descargar datos
                </button>
            </form>

            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger flex-grow-1">
                <i class="bi bi-trash-fill me-1"></i> Eliminar cuenta
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
